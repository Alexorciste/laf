<ul>
  <% @categories.each do |category| %>
    <li><%= category.name %></li>
            <% if category.image.attached? %>
              
                <%= cl_image_tag category.image.key, height: 300, width: 400, crop: :fill %>
                  
              
              <% end %>  
              
    <% end %>
</ul>