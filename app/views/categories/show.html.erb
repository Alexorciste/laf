<div class="container">
  <h1 class="text-center"><%= @category.name %></h1>
 
 <div class = "container"> 
  <div class="cards">
  <% @category.photos.each do |photo|  %>
   
    <% if photo.image.attached? %> 
      
              <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path photo.image.key, height: 300, width: 400, crop: :fill %>')">
              

          <%# <%= link_to category_specialist_path(@category, specialist) do %>
            
            <%# <%= image_tag photo.image%> 

             
                <%= photo.name %> 
                <%= photo.description %>
           
          <%# <% end %> 
        </div>
    <% end%>
   
  

  <% end%>
 </div> 
  </div>

